# Demo Addresses

Not yet captured — this is a Phase 2+/later step (README Build Plan step 8).
Once real addresses are picked for the three demo homes (Coffey Park, a
Malibu canyon home, Paradise), run:

```
./ember "the address" > demo/<slug>.md
```

and record the address, matched Census geocode, and any notable
`partial_failures` or gaps here.
